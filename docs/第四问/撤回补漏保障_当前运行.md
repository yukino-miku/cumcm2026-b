# 第四问当前运行：取消补漏保障

2026-09-13，按用户最新要求关闭自动补漏保障。默认仍使用`run_q4.py`和`q4_reliable.json`，其中`discovery_safeguard=false`。

## 当前流程

沿原圆内13站路线检测，途中继续顺路定位、清除和路线规划。13站结束后处理剩余的已发现目标，完成后退出；不再为未知频道启动外环备选站搜索，不执行12站补漏路线。

保留以下改进：

- 原补测候选被绕行规则拒绝时，查找路线可行替代。
- 对连续位置外包确定超过1500米的已知频道免测。
- 顺路清除、小范围覆盖、方向反馈和已知目标的必要追加补测。
- 主动补测优先间距100米、绕行门槛800米、未知频道顺路扫描间隔600米。

“取消补漏”只关闭未知源保障搜索，不取消已经发现目标的定位补测。程序继续区分流程完成和全清证据，不能用13站结束证明不存在漏源。

## 运行命令

模拟器选择第四问演练，PowerShell执行：

```powershell
Set-Location 'D:\mywork\code\cumcm2026-b'
.\.venv\Scripts\python.exe -X utf8 scripts/run_q4.py --mode http --robot-id '202623003006'
```

无需增加参数，原命令直接使用关闭补漏后的默认配置。已在运行的进程仍使用启动时配置，下一次启动才生效。

## 历史资料

[此前有补漏保障的试验与图表](清除优先版_方法与验证.md)保留作历史复核。其中“默认配置”、25站覆盖证书和8场107/107结果对应开启保障时的版本，不代表本次关闭保障后的成绩。旧配置逐字节保存在`历史代码/q4_reliable_aa0dbbc.json.txt`；不得因历史文档中的“当前”字样自行恢复保障。


## 本次行为复核

通过原入口进行两次本地复核，均未产生保障站或发现保障动作，所有已发现目标处理后正常退出：

|场景|清除|路程|虚拟时间|保障站数|
|---|---|---|---|---|
|109006，10源，边界全部朝外，最小接收半径|0/10|11.400公里|3827.0秒|0|
|109000，10源，均匀混合，最小接收半径|9/10|13.686公里|4116.2秒|0|

这两次用于核对关闭行为，不作为总体性能估计。未发现频道仍保留未知状态，结果没有误报全清；关闭补漏后可能存在未发现源。原入口兼容测试3项通过，覆盖默认、零间距恢复和200米补测间距。本次未连接官方模拟器。
