# B 题原始材料

两个来源在项目内分开归档，来源内部的相对目录结构保持不变：

| 来源目录 | 归档位置 | 正式文件 |
| --- | --- | --- |
| `D:\资料\CUMCM2026\B题` | `original/problem/` | B题.pdf、附件1.docx、附件2.docx，共 3 个 |
| `D:\Tencent Files\3200672080\FileRecv\CUMCM2026B` | `original/simulator/` | 下载说明 PDF、演示 MP4、两个 7z 压缩包，共 4 个 |

两个 `~$` 开头的 Word 锁文件属于临时文件，未复制，排除记录见 `manifest.json`；没有删除或修改来源文件。模拟器来源本身包含一层 `CUMCM2026B/` 子目录，归档保留此层级。

`manifest.json` 使用 schema 2，记录来源列表、归档时间、每个正式文件的来源相对路径、归档相对路径、字节数、SHA256 和存储方式。7 个文件合计 374,503,547 字节；3 个 `.mp4` / `.7z` 文件使用 Git LFS，其余文件使用普通 Git。

用 `scripts/verify-materials.ps1` 核对归档完整性。新克隆先执行 `git lfs pull`，确保获得实际文件。数据处理输出写入 `data/`，不要在此保存修改后的附件。

A 题材料仅保留在历史提交 `e23ca46` 及之前版本；当前选题为 B 题。此阶段仅归档材料，没有读取或执行压缩包中的模拟器，也没有分析题目内容。
