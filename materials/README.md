# 赛题原始材料

将 `D:\资料\CUMCM2026\A题` 内全部文件按原目录结构复制到 `original/`。源目录和归档副本均不作修改。

`manifest.json` 在导入时生成，记录来源目录、归档时间、相对路径、字节数及 SHA256。用 `scripts/verify-materials.ps1` 核对归档完整性。数据处理输出写入 `data/`，不要在此保存修改后的附件。
